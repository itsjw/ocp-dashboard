import { DecryptPinPipe } from './decrypt-pin.pipe';

describe('DecryptPinPipe', () => {
  it('create an instance', () => {
    const pipe = new DecryptPinPipe();
    expect(pipe).toBeTruthy();
  });
});
